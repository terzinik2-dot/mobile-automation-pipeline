{
  "name": "mobile-automation-pipeline",
  "version": "1.0.0",
  "description": "Root package for Mobile Automation Pipeline — starts both FastAPI and Next.js",
  "scripts": {
    "dev": "concurrently \"python api_server.py\" \"cd dashboard && npm run dev\"",
    "build": "cd dashboard && npm run build",
    "start": "concurrently \"python api_server.py\" \"cd dashboard && npm start\"",
    "setup": "pip install -r requirements.txt && cd dashboard && npm install",
    "test": "pytest tests/ -v && cd dashboard && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
